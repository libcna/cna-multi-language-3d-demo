# Python port

Run `python3 python/starfield.py collection` or any scenario from the shared
contract. `ctypes` integration with `c/libcna_starfield.so` can be added by a
host application; this source intentionally keeps the reference behavior
usable without a native library.