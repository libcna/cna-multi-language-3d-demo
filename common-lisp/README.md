# Common Lisp port

With SBCL, run `sbcl --script common-lisp/starfield.lisp win`. The simulation
is renderer-independent and keeps the foreign-function boundary optional for
headless conformance runs.