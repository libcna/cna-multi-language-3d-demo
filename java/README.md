# Java port

Compile and run with `javac Starfield.java && java Starfield win`. The program
is headless so its deterministic behavior can be compared with the C++
reference before a Java desktop renderer is selected.